# JobFix ai-service dependencies
# Install with: pip install -r requirements.txt
# (activate venv first: venv\Scripts\activate)

# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9   # required for FastAPI file uploads

# LLM / LangChain
langchain>=0.3.0
langchain-core>=0.3.0
langchain-groq>=0.3.8
langgraph>=0.2.0

# PDF / DOCX parsing
pymupdf>=1.24.0           # fitz — PDF text extraction
python-docx>=1.1.0

# Data validation
pydantic>=2.7.0

# Environment
python-dotenv>=1.0.0

# PostgreSQL / pgvector
psycopg[binary]>=3.2.0

# Testing
pytest>=8.0.0
